diff a/src/keranjang.php b/src/keranjang.php	(rejected hunks)
@@ -1,6 +1,8 @@
 <?php
 session_start();
 
+include 'koneksiDB.php';
+
 // Add item to cart
 if (isset($_GET['action']) && $_GET['action'] == 'add') {
     $name = $_GET['name'];
